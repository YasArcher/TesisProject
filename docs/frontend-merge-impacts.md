# FRONTEND_MERGE_IMPACTS

Cleanup backend Projects/Articles, 2026-09-09. El frontend no se modifica en esta fase. Se conservan los DTO, wrappers y rutas consumidos; borrar implementaciones backend reemplazadas no exige borrar sus contratos Shared.

| Contrato conservado | Consumidor frontend activo | Semántica Unified | Cambio posterior | Prioridad/riesgo |
| --- | --- | --- | --- | --- |
| `RegisterArticleAggregateRequest.Article.ProductTypeId` / `ArticleAggregateCoreDto` | `Features/Articles/Pages/ArticlesRegister.razor:508`, `BuildRegistrationRequest`; `Services/Implementations/ArticleClientService.cs:52` | Tipo explícito 1 ScientificProduction o 2 RegionalProduction; no se infiere | Poblar `Article.ProductTypeId`: el formulario actual solo inicializa FormKey y no asigna el tipo | Alta: registro actual incompleto |
| `ArticleAggregateCoreDto.ProjectId` / `IsProjectResult` | `ArticlesRegister.razor:579`, `ApplyKnownArticleField` | ProjectId nullable es canónico; IsProjectResult es derivado, no identifica proyecto | Seleccionar ProjectId real o null; reemplazar el booleano como entrada autoritativa. Actualmente solo se envía IsProjectResult | Alta: booleano no permite resolver proyecto |
| `RegisterArticleAggregateResponse.ProductId`, `ArticleId` | `Services/Implementations/ArticleClientService.cs:52`; `Services/Interfaces/IArticleClientService.cs:15`; envío desde `ArticlesRegister.razor` | ProductId identifica producto; ArticleId identifica extensión estructural | Mantener ambos y usar cada ID según contrato del endpoint; no intercambiarlos | Alta al agregar navegación/edición |
| `RegisterArticleAggregateResponse.ParticipantIds` | Mismo wrapper `HttpResponseWrapper<RegisterArticleAggregateResponse?>` en ArticleClientService | Son `ProductAuthor.Id`, no participantes legacy ni Author.Id | Documentar y adaptar futuros enlaces/edición por participante. No se detectó uso individual actual de estos IDs en la página de registro | Media, riesgo de identidad incorrecta |
| `ArticleParticipantAggregateDto` | `ArticlesRegister.razor:524`, construcción de participantes | Institucional: InstitutionalPersonId de directorio validado → boundary Unified → AppUser → Author. Externo: ExternalResearcher → Author. ExternalAuthorId solo legacy | El constructor actual solo llena snapshots, orden, rol y principal. Agregar selección institucional/externa y datos de resolución; no relink por email ni asumir equivalencia entre IDs | Alta: snapshots no sustituyen identidad |
| `ArticleParticipantAggregateDto.DynamicFields`, `Index`, `IsPrimaryAuthor` | `ArticlesRegister.razor:524` | DynamicFields se persisten en ProductAuthorDynamicFieldValue; Index es AuthorOrder; principal sigue independiente del rol/orden en backend | Conectar valores dinámicos de participante al request. Revisar con negocio la elección UI `index == 0 || Role == Autor`; no se cambia esa regla durante cleanup | Media |
| `ArticleAggregateCoreDto`, `ArticleVenueInputDto`, `ArticleVenueMetricInputDto` | `ArticlesRegister.razor:540`, `ApplyKnownArticleField` | DOI/Year/URL y atributos base provienen de ProductValues; Venue es estructura, no segunda fuente autoritativa de atributos | Mantener payload compatible y mapear los campos base del formulario a entradas canónicas; no crear campos dinámicos duplicados | Alta para futura configuración |
| `ResolvedFormDto`, `ResolvedFormFieldDto` | `Services/Implementations/FormConfigurationClient.cs:35`; `ArticlesRegister.razor`; `ArticlesRegistrationMatrix.razor:142` | Un único selector efectivo Unified; mismas rutas `/config/forms/{key}/resolved` y resolved-active | No cambio contractual de cleanup; atender configuración ausente sin inventar forms/fields | Media: baseline inexistente |
| DTO de matrices, `RegistrationMatrixDetailDto` | `Services/Implementations/RegistrationMatrixClient.cs:14`; `Features/Articles/Pages/ArticlesRegistrationMatrix.razor:144` | CRUD/ownership/CanManageAll sobre Unified, mismas rutas | Conservar cliente y gestión de ErrorCode/ValidationErrors; no Staging nuevo | Baja |
| `CurrentSessionResponse` | `Services/Implementations/AuthClientService.cs:40`; `Pages/Login.razor.cs:82`; `Features/Articles/Pages/ArticlesProfile.razor:121` | auth/me usa Identity local → AppUser.IdLocal; AppUserId nullable cuando no hay mapping válido | Conservar DTO y tratamiento de ausencia de mapping; no sustituir IdAsp/IdUser por email | Media |
| `ProductCreateRequestDTO`, `ProductUpdateRequestDTO`, `ProductValueUpsertDTO`, `ProductListItemResponseDTO` | `Features/Projects/Components/ProjectProductsEditor.razor:660,832` | ProjectId/ProductTypeId y AttributeDefinitionId resueltos por catálogos; contratos reutilizados por UnifiedProductService | Mantener catálogo dinámico; no confundir AttributeDefinitionId con ProductAttributeId base. Hoy AuthorUserIds se envía vacío desde creación | Baja; no cambio de contrato en cleanup |
| DTO de Projects, catálogos, wrappers `ServiceResult` / `HttpResponseWrapper` | Clientes de `tesisproject.frontend/Services` y páginas activas de `Features/Projects` | Controllers Unified preservan DTO, binding, autorización y rutas; snapshot de contratos antes del cleanup | Ningún cambio por eliminación de implementaciones legacy. Revisar cambios funcionales futuros por separado | Baja |

Rutas y contratos se verifican con `tests/controller-contracts-before-cleanup.json` y las pruebas HTTP/DI. Shared se conserva; este reporte no autoriza modificar frontend.

El smoke completo de registro Articles con datos reales permanece **NOT_EXECUTED** por ausencia de configuración/base de Articles, aceptado expresamente como no bloqueante para cleanup. No se inventaron seeds, formularios ni fields.
