@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Redirige inmediatamente a /login al entrar a la raíz
        Nav.NavigateTo("/login", forceLoad: true);
    }
}
