@using tesisproject.shared.DTOs.Catalog.Common.Response
@using tesisproject.frontend.SharedUI.ActionButton

<div class="group rounded-xl border-2 border-border bg-background shadow-sm hover:shadow-md transition-all duration-200 h-[28rem] flex flex-col overflow-hidden">
    <div class="p-5 border-b border-border bg-background/50 shrink-0">
        <div class="flex flex-col gap-3">
            <div class="flex items-start justify-between gap-4">
                <div class="flex items-center gap-2 min-w-0">
                    @if (Icon is not null)
                    {
                        <div class="h-5 w-5 text-primary flex-shrink-0">
                            @Icon
                        </div>
                    }

                    <h3 class="text-base font-semibold text-foreground truncate">
                        @Title
                    </h3>
                </div>

                <div class="flex items-center gap-2 flex-wrap justify-end flex-shrink-0">
                    <span class="inline-flex items-center px-2.5 py-1 rounded-md bg-primary/10 text-xs font-medium text-primary whitespace-nowrap">
                        @VisibleItems.Count / @Items.Count @(Items.Count == 1 ? CountLabelSingular : CountLabelPlural)
                    </span>

                    @if (InactiveCount > 0)
                    {
                        <ActionButton Text="@(_showInactive? HideInactiveText : ShowInactiveText)"
                                      Intent="ButtonIntent.Add"
                                      Variant="ButtonVariant.Outline"
                                      Size="ButtonSize.Xs"
                                      Execute="@ToggleInactive"
                                      Class="opacity-80 hover:opacity-100">
                            <Icon>
                                @if (_showInactive)
                                {
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                                        <path d="M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z" />
                                        <path fill-rule="evenodd" d="M1.323 11.447C2.811 6.976 7.028 3.75 12.001 3.75c4.97 0 9.185 3.223 10.675 7.69.12.362.12.752 0 1.113-1.487 4.471-5.705 7.697-10.677 7.697-4.97 0-9.186-3.223-10.675-7.69a1.762 1.762 0 0 1 0-1.113ZM17.25 12a5.25 5.25 0 1 1-10.5 0 5.25 5.25 0 0 1 10.5 0Z" clip-rule="evenodd" />
                                    </svg>
                                }
                                else
                                {
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                                        <path d="M3.53 2.47a.75.75 0 0 0-1.06 1.06l18 18a.75.75 0 1 0 1.06-1.06l-18-18ZM22.676 12.553a11.249 11.249 0 0 1-2.631 4.31l-3.099-3.099a5.25 5.25 0 0 0-6.71-6.71L7.759 4.577a11.217 11.217 0 0 1 4.242-.827c4.97 0 9.185 3.223 10.675 7.69.12.362.12.752 0 1.113Z" />
                                        <path d="M15.75 12c0 .18-.013.357-.037.53l-4.244-4.243A3.75 3.75 0 0 1 15.75 12ZM12.53 15.713l-4.243-4.244a3.75 3.75 0 0 0 4.244 4.243Z" />
                                        <path d="M6.75 12c0-.619.107-1.213.304-1.764l-3.1-3.1a11.25 11.25 0 0 0-2.63 4.31c-.12.362-.12.752 0 1.114 1.489 4.467 5.704 7.69 10.675 7.69 1.5 0 2.933-.294 4.242-.827l-2.477-2.477A5.25 5.25 0 0 1 6.75 12Z" />
                                    </svg>
                                }
                            </Icon>
                        </ActionButton>
                    }

                    <ActionButton Text="@NewButtonText"
                                  Intent="ButtonIntent.Add"
                                  Variant="ButtonVariant.Outline"
                                  Size="ButtonSize.Xs"
                                  Execute="OnNewRequested">
                        <Icon>
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                                <path fill-rule="evenodd" d="M12 3.75a.75.75 0 0 1 .75.75v6.75h6.75a.75.75 0 0 1 0 1.5h-6.75v6.75a.75.75 0 0 1-1.5 0v-6.75H4.5a.75.75 0 0 1 0-1.5h6.75V4.5a.75.75 0 0 1 .75-.75Z" clip-rule="evenodd" />
                            </svg>
                        </Icon>
                    </ActionButton>
                </div>
            </div>

            @if (!string.IsNullOrWhiteSpace(Description))
            {
                <div>
                    <p class="text-xs text-muted leading-relaxed">
                        @Description
                    </p>
                </div>
            }
        </div>
    </div>

    <div class="p-5 flex-1 min-h-0 overflow-hidden">
        @if (VisibleItems.Count == 0)
        {
            <div class="h-full min-h-0 flex flex-col items-center justify-center text-center px-4">
                @if (EmptyIcon is not null)
                {
                    <div class="mx-auto h-12 w-12 text-muted/40 mb-3">
                        @EmptyIcon
                    </div>
                }
                else
                {
                    <svg class="mx-auto h-12 w-12 text-muted/40 mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4" />
                    </svg>
                }

                <p class="text-sm font-medium text-muted">
                    @EmptyTitle
                </p>

                @if (!string.IsNullOrWhiteSpace(EmptySubtitle))
                {
                    <p class="mt-1 text-xs text-muted/70 max-w-xs">
                        @EmptySubtitle
                    </p>
                }

                @if (!_showInactive && InactiveCount > 0)
                {
                    <p class="mt-2 text-xs text-muted/70">
                        Hay @InactiveCount inactivo(s) oculto(s). Use "@ShowInactiveText".
                    </p>
                }
            </div>
        }
        else
        {
            <ul class="space-y-2 h-full min-h-0 overflow-y-auto pr-1 scrollbar-thin">
                @foreach (var item in VisibleItems)
                {
                    <li class="group/item rounded-lg border border-border bg-background hover:border-primary/40 hover:bg-primary/5 transition-all duration-150">
                        <div class="flex items-center justify-between gap-3 p-3">
                            <div class="flex flex-col flex-1 min-w-0">
                                <span class="truncate font-medium text-sm text-foreground mb-1.5">
                                    @item.Name
                                </span>

                                <div class="flex items-center gap-1.5 flex-wrap">
                                    @if (item.IsActive)
                                    {
                                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-md text-xs font-medium bg-secondary/15 text-secondary border border-secondary/30">
                                            <svg class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                            </svg>
                                            Activo
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-md text-xs font-medium bg-muted/15 text-muted border border-border">
                                            <svg class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                            </svg>
                                            Inactivo
                                        </span>
                                    }
                                </div>
                            </div>

                            <div class="flex items-center gap-2 flex-shrink-0">
                                @if (!item.IsLocked)
                                {
                                    <ActionButton Intent="ButtonIntent.Add"
                                                  Variant="ButtonVariant.Outline"
                                                  Size="ButtonSize.Xs"
                                                  Execute="@(() => OnEditRequested.InvokeAsync(item.Id))"
                                                  Class="opacity-80 hover:opacity-100">
                                        <Icon>
                                            @if (EditIcon is not null)
                                            {
                                                @EditIcon
                                            }
                                        </Icon>
                                    </ActionButton>

                                    <ActionButton Intent="ButtonIntent.Delete"
                                                  Variant="ButtonVariant.Outline"
                                                  Size="ButtonSize.Xs"
                                                  ConfirmBeforeExecute="true"
                                                  ConfirmTitle="@DeleteConfirmTitle"
                                                  ConfirmMessage="@DeleteConfirmMessage"
                                                  ConfirmButtonText="@DeleteConfirmButtonText"
                                                  CancelButtonText="@DeleteCancelButtonText"
                                                  Execute="@(() => OnDeleteRequested.InvokeAsync(item.Id))"
                                                  Class="opacity-80 hover:opacity-100">
                                        <Icon>
                                            @if (DeleteIcon is not null)
                                            {
                                                @DeleteIcon
                                            }
                                        </Icon>
                                    </ActionButton>
                                }
                                else
                                {
                                    <div class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md text-xs font-medium bg-muted/10 text-muted/60">
                                        <svg class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                                        </svg>
                                        Protegido
                                    </div>
                                }
                            </div>
                        </div>
                    </li>
                }
            </ul>
        }
    </div>
</div>

@code {
    [Parameter] public IReadOnlyList<CatalogListItemDTO> Items { get; set; } = Array.Empty<CatalogListItemDTO>();

    [Parameter, EditorRequired] public string Title { get; set; } = string.Empty;
    [Parameter] public string Description { get; set; } = string.Empty;
    [Parameter] public string NewButtonText { get; set; } = "Nuevo elemento";

    [Parameter] public string CountLabelSingular { get; set; } = "elemento";
    [Parameter] public string CountLabelPlural { get; set; } = "elementos";

    [Parameter] public string EmptyTitle { get; set; } = "Aún no hay elementos";
    [Parameter] public string EmptySubtitle { get; set; } = "Cree elementos para comenzar a usar este catálogo.";

    [Parameter] public string DeleteConfirmTitle { get; set; } = "Eliminar elemento";
    [Parameter] public string DeleteConfirmMessage { get; set; } = "¿Está seguro de que desea eliminar este elemento? Esta acción no se puede deshacer.";
    [Parameter] public string DeleteConfirmButtonText { get; set; } = "Eliminar";
    [Parameter] public string DeleteCancelButtonText { get; set; } = "Cancelar";

    [Parameter] public RenderFragment? Icon { get; set; }
    [Parameter] public RenderFragment? EmptyIcon { get; set; }
    [Parameter] public RenderFragment? EditIcon { get; set; }
    [Parameter] public RenderFragment? DeleteIcon { get; set; }

    [Parameter] public EventCallback OnNewRequested { get; set; }
    [Parameter] public EventCallback<int> OnEditRequested { get; set; }
    [Parameter] public EventCallback<int> OnDeleteRequested { get; set; }

    [Parameter] public bool HideInactiveByDefault { get; set; } = true;
    [Parameter] public string ShowInactiveText { get; set; } = "Mostrar inactivos";
    [Parameter] public string HideInactiveText { get; set; } = "Ocultar inactivos";

    private bool _showInactive;

    protected override void OnInitialized()
    {
        _showInactive = !HideInactiveByDefault;
    }

    private IReadOnlyList<CatalogListItemDTO> VisibleItems =>
        _showInactive ? Items : Items.Where(x => x.IsActive).ToList();

    private int InactiveCount => Items.Count(x => !x.IsActive);

    private Task ToggleInactive()
    {
        _showInactive = !_showInactive;
        return Task.CompletedTask;
    }
}