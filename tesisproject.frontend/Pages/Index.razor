@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Cambia la ruta de destino
        Nav.NavigateTo("/projects/home", true);
    }
}
