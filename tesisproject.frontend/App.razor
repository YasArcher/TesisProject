@using Microsoft.AspNetCore.Components.Authorization
@using tesisproject.frontend.Features.Auth

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData"
                                DefaultLayout="@(typeof(tesisproject.frontend.Features.Management.Layout.MainLayout))">
                <NotAuthorized>
                    <LayoutView Layout="@(typeof(tesisproject.frontend.Features.Management.Layout.AuthLayout))">
                        <AuthAccessGate />
                    </LayoutView>
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>

        <NotFound>
            <LayoutView Layout="@(typeof(tesisproject.frontend.Features.Management.Layout.MainLayout))">
                <p>Página no encontrada</p>
                <a href="/">Ir al sistema</a>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>
